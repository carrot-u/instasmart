<div>
  <h4>Submitted by <%= link_to answer.user.full_name, user_path(answer.user.id) %> on <%= answer.created_at %></h4>
  <p><%= answer.body %></p>
  <p><%=  answer.votes_for.count %> Votes <button><%= link_to 'Add Vote', like_question_answer_path(@question, answer), method: :put %></button> <button><%= link_to 'Remove Vote', unlike_question_answer_path(@question, answer), method: :put %></button></p>
  <% if answer.comments.count > 0 %>
    <strong>Comments</strong>
    <h6><%= render partial: 'questions/comment', collection: answer.comments %></h6>
    <% end %>
  </div>
